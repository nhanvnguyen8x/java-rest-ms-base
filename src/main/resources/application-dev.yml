spring:
  datasource:
    url: jdbc:postgresql://core.scifn.com:5432/modelex?currentSchema=modelex_ai
    username: modelex_user
    password: modelex321122x!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    ignore-unsupported: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

server:
  ssl:
    enable: false